Using skills: S40_PROCESS_IMPLEMENTATION, S50_ARTIFACT_AND_IO, S60_EVALUATION_QC to add a new process with artifact outputs and QC-style distance scoring.

Implemented `process=calibrate_search` with a deterministic random-search loop that generates synthetic particles in-memory, scores histogram distances vs real inputs, and writes `trials.csv`, `best_score.json`, and `config/best_config.yaml`; added default configs and a reproducibility test to confirm improvement over baseline. Updated `docs/10_PROCESS_CATALOG.md` to document the new process contract.

Tests run: `pytest -q -k T0402`

Next steps you may want:
1) Run the full suite with `pytest -q`
2) Try `process=calibrate_search` against a real dataset path and a richer search space

Files changed:
- `src/synthlab/processes/calibrate_search.py`
- `src/synthlab/processes/__init__.py`
- `conf/process/calibrate_search.yaml`
- `conf/wafer_particles/calibrate_search/default.yaml`
- `docs/10_PROCESS_CATALOG.md`
- `tests/test_T0402_distance_minimization_calibration_loop.py`