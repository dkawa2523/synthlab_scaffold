Added `process=compare` to load two run artifacts and compute run-to-run distance metrics (JS/KL for r/theta, KS/Wasserstein for size, polar density L1/L2, NN KS), writing `compare_summary.json`, `compare_table.csv`, and a histogram overlay plot with sanitized input metadata; wired new compare config defaults, process registration, a self-consistency test, and documented the process in the catalog.
- Metrics implementation and outputs live in `src/synthlab/processes/compare.py`, and the process is registered in `src/synthlab/processes/__init__.py`.
- Default compare config (fixed bins, smoothing epsilon, NN sampling cap) is in `conf/compare/default.yaml` and loaded via `conf/process/compare.yaml`.
- Self-consistency test for near-zero distances is in `tests/test_T0204_compare_distance_metrics.py`.
- Process catalog updated in `docs/10_PROCESS_CATALOG.md`.

**Files Changed**
- `src/synthlab/processes/compare.py`
- `src/synthlab/processes/__init__.py`
- `conf/compare/default.yaml`
- `conf/process/compare.yaml`
- `docs/10_PROCESS_CATALOG.md`
- `tests/test_T0204_compare_distance_metrics.py`

**Verification**
- Not run here. Suggested commands:
```bash
python -m synthlab.cli.main process=generate wafer_particles.n_samples=30 seed=10
python -m synthlab.cli.main process=generate wafer_particles.n_samples=30 wafer_particles/patterns=ring_wide seed=11
python -m synthlab.cli.main process=compare compare.a_run="runs/<runA>/generate" compare.b_run="runs/<runB>/generate" seed=12
python -m synthlab.cli.main process=compare compare.a_run="runs/<runA>/generate" compare.b_run="runs/<runA>/generate" seed=13
python - << 'PY'
import json, glob
p = sorted(glob.glob("runs/*/compare/metrics/compare_summary.json"))[-1]
d = json.load(open(p))
assert "r_hist_js" in d
assert "theta_hist_js" in d
assert "size_ks" in d
assert "size_wasserstein" in d
print("OK:", p)
PY
pytest -q tests/test_T0204_compare_distance_metrics.py
```

Next steps you might want:
1) Run the verification commands above
2) Run the full test suite with `pytest -q`